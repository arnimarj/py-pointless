language: python
python:
  - "2.6"
  - "2.7"
before_install:
  - sudo apt-get install libjudy-dev
install:
  - "pip install ."
  - "pip install -r test-requirements.txt"

script:
  - "trial test.python_api"
