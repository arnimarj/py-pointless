[tool.cibuildwheel]
test-command = [
  "python {package}/sanity-check.py",
]

[tool.cibuildwheel.linux]
environment = "CC=gcc"

[[tool.cibuildwheel.overrides]]
select = "*-musllinux*"
environment = "CC=gcc"
