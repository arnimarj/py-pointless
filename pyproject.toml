[tool.cibuildwheel.linux]
before-all = "yum install -y clang && unset CC"
test-command = "which clang"

[[tool.cibuildwheel.overrides]]
select = "*-musllinux*"
before-all = "export CC=gcc"
test-command = "if [[ ! -v CC ]]; then false; fi"
